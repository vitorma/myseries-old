<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp">

    <ImageView
        android:id="@+id/seriesImageView"
        android:layout_width="61dp"
        android:layout_height="90dp"
        android:scaleType="centerInside"
        android:layout_marginRight="5dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:src="@drawable/small_poster_clapperboard"/>

    <TextView
        android:id="@+id/nameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/seriesImageView"
        android:ellipsize="marquee"
        android:lines="1"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:text="@string/loading"
        android:textAppearance="?android:attr/textAppearanceMedium"/>

    <mobi.myseries.gui.SeenEpisodesBar
        android:id="@+id/SeenEpisodesBar"
        android:layout_width="match_parent"
        android:layout_height="9dp"
        android:layout_marginTop="8dp"
        android:layout_below="@id/nameTextView"
        android:layout_toRightOf="@id/seriesImageView"/>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/SeenEpisodesBar"
        android:layout_toRightOf="@id/seriesImageView">

        <include
            android:id="@+id/nextToSeePanel"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            layout="@layout/myseries_item_nexttosee"/>

        <include
            android:id="@+id/nextToSeeUpToDatePanel"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            layout="@layout/myseries_item_nexttosee_uptodate"/>

    </FrameLayout>

</RelativeLayout>